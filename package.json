{
  "name": "ff-workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace winfox",
    "test": "npm run test --workspace winfox"
  }
}
